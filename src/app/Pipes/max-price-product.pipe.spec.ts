import { MaxPriceProductPipe } from './max-price-product.pipe';

describe('MaxPriceProductPipe', () => {
  it('create an instance', () => {
    const pipe = new MaxPriceProductPipe();
    expect(pipe).toBeTruthy();
  });
});

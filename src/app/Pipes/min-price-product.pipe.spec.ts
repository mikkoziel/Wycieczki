import { MinPriceProductPipe } from './min-price-product.pipe';

describe('MinPriceProductPipePipe', () => {
  it('create an instance', () => {
    const pipe = new MinPriceProductPipe();
    expect(pipe).toBeTruthy();
  });
});
